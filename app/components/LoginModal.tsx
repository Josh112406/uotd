/**
 * LoginModal — deprecated after Supabase auth was introduced.
 * Kept as an empty stub so any lingering imports don't break the build.
 * Safe to delete from your project entirely.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export default function LoginModal(props: { isOpen: boolean; onClose: () => void }) {
  return null;
}
